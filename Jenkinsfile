pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Testing 1'
                sh "whoami && pwd"
                // Add your build steps here
            }
        }
    }
}
