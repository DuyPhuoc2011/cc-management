pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Building...'
                sh "whoami && pwd"
                // Add your build steps here
            }
        }
    }
}
