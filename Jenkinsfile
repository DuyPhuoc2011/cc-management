pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Testing'
                sh "whoami && pwd"
                // Add your build steps here
            }
        }
    }
}
